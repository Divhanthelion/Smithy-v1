---
name: grill-me
description: A relentless interview to sharpen a plan or design.
argument-hint: "The plan or design to grill"
tools: [read, ls, glob, grep, web_fetch, web_search, symbol, explore]
include: grilling.md, rust-first.md
---

Run a grilling session. `read`, `explore`, `ls`, `glob`, `grep`, `symbol`, `web_fetch`, and `web_search` are generally suited to this task.

Recommend Rust answers on the frontier. If a recommended answer leaves Rust, it must say why it is impossible to stay.
